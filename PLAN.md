# 테트리스 MVP 웹앱 생성 계획

## Context

`src/exercise/jangwonbok/day02/tetris/` 디렉터리에는 처음에 `AGENT.md`만 존재하고 실제 코드는 없었다. 사용자는 브라우저에서 바로 실행 가능한 간단한 프론트엔드 테트리스 게임을 요청했으며, 고급 기능 없이 블록 이동·점수 표시 등 최소 기능만 있는 MVP를 원했다.

리포지토리 컨벤션 조사 결과(Explore 에이전트 + 직접 확인):
- `day01`의 정적 사이트류 예제(`jangwonbok/day01/personal_landing` 등)는 빌드 도구 없이 `index.html` / `style.css` / `script.js` 3파일 구조 + `README.md`를 사용한다. 이 패턴을 그대로 따랐다.
- `day02/tetris/AGENT.md`는 다음을 요구한다: (1) 모든 프롬프트(원본)와 결과(요약)를 `WORKFLOW.md`에 기록, (2) 원격 변경 시 rebase 대신 merge 사용.
- `bardroh/day02/memo/WORKFLOW.md`가 템플릿: 각 항목을 `## N. 제목` → `### 프롬프트 (원본)` (코드블록에 원문 그대로) → `### 결과 (요약)` (불릿 요약) 형식으로 기록한다.

## 목표 산출물 (완료 상태)

`src/exercise/jangwonbok/day02/tetris/` 아래 파일:

1. **`index.html`** ✅ — 캔버스 기반 게임 보드(10x20 그리드), 점수 표시 영역, 조작 안내, 재시작 버튼.
2. **`style.css`** ✅ — 레이아웃, 색상, 게임오버 오버레이 등 최소 스타일.
3. **`script.js`** ✅ — 게임 로직 전체 (아래 "MVP 기능" 참조). HTML5 `<canvas>` 2D 컨텍스트로 렌더링.
4. **`README.md`** ✅ — 프로젝트 설명, 파일 구성, 실행 방법, 조작키 안내.
5. **`WORKFLOW.md`** ✅ — AGENT.md 규칙에 따라 프롬프트 원본/결과 요약 기록.
6. **`PLAN.md`** ✅ — 본 계획 문서.

## MVP 기능 범위

**포함:**
- 7종 표준 테트로미노(I, O, T, S, Z, J, L)와 각 고유 색상
- 10열 x 20행 보드, 다음 블록이 위에서 스폰
- 키보드 조작: ← → (좌우 이동), ↓ (소프트 드롭), ↑ (회전), Space (하드 드롭)
- 충돌/경계 검사, 바닥/블록 도달 시 고정(lock)
- 줄 완성 시 해당 줄 제거 + 위 블록 낙하 + 점수 가산 (100 x 동시 제거 줄 수)
- 화면에 현재 점수 표시
- 게임오버 판정(스폰 위치가 막히면 종료) + "Game Over" 표시 + 재시작 버튼

**제외 (고급 기능, MVP 범위 아님):**
- 다음 블록 미리보기, 홀드 기능, 고스트 피스, 레벨/속도 증가, 사운드, 로컬 랭킹 저장, 터치/모바일 조작

## 구현 메모

- 순수 Vanilla JS, 프레임워크/빌드 도구 없음 (리포 컨벤션과 일치)
- 게임 루프는 `requestAnimationFrame` + 낙하 간격 타이머(고정 tick, 800ms)로 구현
- 보드 상태는 2차원 배열로 관리, 매 프레임 canvas를 clear 후 보드+현재 피스 렌더링

## 검증 방법 및 결과

- `python3 -m http.server`로 `tetris/` 디렉터리에서 로컬 서버 실행 후 확인 가능
- 이 실행 환경에는 실제 브라우저 창과 headless Chromium 구동에 필요한 시스템 라이브러리가 없어, jsdom으로 headless DOM에 `script.js`를 로드하고 실제 키 이벤트(←/→/↑/Space)를 발생시켜 검증함:
  - 초기 상태 정상
  - 좌우 이동/회전 시 예외 없음
  - 하드 드롭 반복 시 블록이 정상적으로 쌓이고 게임오버가 감지됨
  - 재시작 버튼 클릭 시 점수·오버레이가 초기화됨
- canvas 렌더링(색상/그래픽 표시)은 육안으로 직접 확인하지 못했으므로, 실제 브라우저에서 한 번 시각적으로 확인 권장
